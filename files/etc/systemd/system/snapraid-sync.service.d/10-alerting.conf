[Unit]
OnFailure=ntfy-alert@%n.service

[Service]
# Ping only on success. A failed run leaves the check un-pinged, so the
# dead-man switch reports it even if the OnFailure push never got out.
ExecStartPost=/usr/local/bin/hc-ping HC_SNAPRAID_SYNC_URL
