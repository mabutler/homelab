[Unit]
Description=Nightly SnapRAID sync

[Timer]
OnCalendar=*-*-* 03:00:00
RandomizedDelaySec=15m
# If the machine was off at 03:00, sync when it next comes up rather than
# skipping a night.
Persistent=true

[Install]
WantedBy=timers.target
