# 05:00 rather than podman's default midnight: the nightly SnapRAID sync starts
# at 03:00 and a container restart mid-sync is noise in the diff for no reason.
[Timer]
OnCalendar=
OnCalendar=*-*-* 05:00:00
RandomizedDelaySec=30m
Persistent=true
